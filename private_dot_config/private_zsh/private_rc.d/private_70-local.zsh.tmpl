# ~/.config/zsh/rc.d/70-local.zsh

# Machine-local / externally-managed shell shims.

# >>> minimal >>>
[ -f "$HOME/.local/share/minimal/shell-init/zsh.sh" ] && . "$HOME/.local/share/minimal/shell-init/zsh.sh"
# <<< minimal <<<

{{- if .workMode }}
# persona managed zshrc for the npq wrapper
[ -f "$HOME/.zshrc" ] && . "$HOME/.zshrc"
{{- end }}
